{
    "project_name": "Project Name",
    "project_slug": "{{ cookiecutter.project_name.lower()|replace(' ', '_')|replace('-', '_') }}",
    "use_heroku": "y",
    "heroku_slug": "{% if cookiecutter.use_heroku == 'y' %}{{ cookiecutter.project_slug.lower()|replace('_', '-') }}{% else %}ignored{% endif %}",
    "use_uploadcare": "y",
    "use_geo": "y"
}
